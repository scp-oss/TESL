PyQt6>=6.5
requests>=2.31
urllib3>=2.0
vdf>=3.4          # опционально: точный парсинг Steam libraryfolders.vdf (иначе regex fallback)
pyinstaller>=6.0  # только для сборки .exe, не нужен для запуска из исходников
